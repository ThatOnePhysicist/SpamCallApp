[package]
name = "spam_logger"
version = "0.1.0"
edition = "2021"

[lib]
name = "spam_logger"
crate-type = ["cdylib"]

[dependencies]
jni = "0.21"          # JNI for Android integration
chrono = { version = "0.4", features = ["clock"] } # For timestamps
